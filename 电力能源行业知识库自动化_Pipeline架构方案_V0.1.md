# 电力能源行业知识库自动化 Pipeline 架构方案（V0.1）

## 一、项目目标

构建一套可长期运行的行业知识资产流水线，实现：

```text
网站 → PDF → 查重 → 分类 → 本地书架 → 飞书索引 → 批次交付
```

最终达到：

- AI 自动发现最新行业标准
- 自动下载 PDF
- 自动查重
- 自动分类
- 自动归档
- 自动生成工作汇报
- 人工只处理异常情况

形成稳定的知识库生产体系。

---

# 二、当前业务背景

当前主要聚焦：

- 电力能源行业
- 国家标准
- 行业标准
- 企业标准
- 电力政策

资料来源：

- 学兔兔
- 国家标准网站
- 电网企业标准平台
- 行业协会网站
- 其他标准资料网站

当前已有：

- **1,313 条记录（第0批次，✅ 已完成飞书索引建立）**
  - 国家标准 252 条 / 行业标准 573 条 / 企业标准 283 条 / 电力政策 205 条
  - 来源：`标准规范引用名录一览表1.14.xlsx`（戴总提供）
- **飞书多维表格（第0批次索引）**：`第0批次_知识库标准目录`，1,313 条记录，已与 Excel 完全核对一致
- **旧表保留**：`知识库PDF清单分类`，1,204 条（两批：4.22标准 + 政策监控），不删不改，作为历史参考
- **仪表盘**：`第0批次_知识库统计`，5 张图表（总量指标卡 + 标准类型环形图 + 阶段条形图 + 交叉柱状图 + 状态饼图），支持下钻
- 2.4GB 行业 PDF 资料（本地未归档，待 Pipeline 2/3 处理）
- Playwright 浏览器自动化体系
- 浏览器 HTTP API
- Cookie 持久化
- 飞书协同能力

---

# 三、核心业务闭环

## 整体流程

```text
外部网站
  ↓
采集 Pipeline
  ↓
临时下载区 Inbox
  ↓
查重与标准化 Pipeline
  ↓
分类与归档 Pipeline
  ↓
本地 Finder 目录
  ↓
飞书多维表格索引
  ↓
交付给戴总小程序
```

---

# 四、双账本体系

## 1. Finder（本地书架）

负责：

- 存放真实 PDF
- 形成清晰行业目录
- 对应业务流程结构

### 推荐目录结构

目录命名统一采用 `序号_名称` 格式（下划线前缀，方便脚本排序），与 `directory_schema_v1.json` 对应：

```text
电力能源知识库/
├── 01_发电/
│   ├── 01_光伏发电/
│   │   ├── 01_通用/
│   │   │   ├── 01_国家标准/
│   │   │   ├── 02_行业标准/
│   │   │   ├── 03_地方标准/
│   │   │   └── 04_企业标准/
│   │   ├── 02_勘察/
│   │   ├── 03_设计/
│   │   ├── 04_造价/
│   │   ├── 05_施工验收/
│   │   ├── 06_运维/
│   │   └── 07_安全/
│   ├── 02_风力发电/
│   ├── 03_火力发电/
│   ├── 04_水力发电/
│   ├── 05_垃圾发电/
│   ├── 06_氢气发电/
│   └── 07_核电/
├── 02_输电/
│   ├── 01_通用/
│   │   ├── 01_国家标准/
│   │   ├── 02_行业标准/
│   │   ├── 03_地方标准/
│   │   └── 04_企业标准/
│   ├── 02_勘察/
│   ├── 03_设计/
│   ├── 04_造价/
│   ├── 05_施工验收/
│   ├── 06_运维/
│   └── 07_安全/
├── 03_变电/        （同输电结构）
├── 04_配电/        （同输电结构）
└── 05_用电/        （同输电结构）
```

---

## 2. 飞书多维表格（索引检索机）

负责：

- 文件索引
- 查重
- 状态追踪
- 批次管理
- 小程序交付管理

### 当前字段（实际使用）

| 字段 | 类型 | 枚举值 / 说明 |
|---|---|---|
| ID | 自动编号 | 唯一标识，系统自动生成 |
| 文件存在 | 复选框 | 本地文件是否存在（Index Agent 写入并定期校验）|
| 板块 | **单选（枚举）** | 发电 / 输电 / 变电 / 配电 / 用电 |
| 项目 | **单选（枚举）** | 光伏发电 / 风力发电 / 火力发电 / 水力发电 / 垃圾发电 / 氢气发电 / 核电 /（输电/变电等板块可留空）|
| 阶段 | **单选（枚举）** | 通用 / 勘察 / 设计 / 造价 / 施工验收 / 运维 / 安全 |
| 标准类型 | **单选（枚举）** | 国家标准 / 行业标准 / 地方标准 / 企业标准 |
| 批次 | 文本 | 批次号，格式：YYYYMM，如 202605 |
| 标准名称 | 文本 | PDF 标准全称，如"光伏发电站设计规范" |
| 标准编号 | 文本 | GB/T、DL/T、Q/GDW 等，如 GB 50797-2012 |
| 分类链 | 文本 | 由上方4个枚举字段自动拼接，格式：板块/项目/阶段/标准类型 |
| 本地路径 | 文本 | 绝对路径，含根目录，精确到文件名 |
| 文件大小MB | 数字 | 文件大小 |
| 入库状态 | 单选 | 新增 / 完全重复 / 疑似重复 / 版本更新 / 待处理 |
| 备注 | 文本 | 异常说明、人工复核意见 |

> **关键设计**：板块、项目、阶段、标准类型四个字段均为下拉单选，枚举值严格来自 `directory_schema_v1.json`（唯一数据源，见第五章 Pipeline 3）。分类链由这四个字段自动拼接，直接对应本地 Finder 目录路径的后四级。任何新增枚举值必须先修改 JSON 文件，禁止在飞书中手动添加下拉选项。

### 需补充字段（查重与交付必需）

| 字段 | 类型 | 枚举值 / 说明 |
|---|---|---|
| 文件Hash | 文本 | SHA256，查重核心 |
| 来源URL | 文本 | 下载来源网址 |
| 下载时间 | 日期 | 入库时间戳 |
| 人工确认分类 | 复选框 | MVP 阶段强制人工复核，勾选后方可导入小程序 |
| 导入状态 | 单选 | 待导入 / 已导入 / 不导入 |

### 本地路径规范

本地路径统一存储**绝对路径**，必须包含根目录，精确到文件名，确保任何人在任何时候都能直接定位文件：

```text
✅ 正确（完整绝对路径）：
/Users/mf/Desktop/电力能源知识库/01_发电/01_光伏发电/03_设计/01_国家标准/GB_50797-2012_光伏发电站设计规范.pdf

❌ 错误（相对路径，无法独立定位）：
01_发电/01_光伏发电/03_设计/GB_50797-2012.pdf
```

**路径字段填写要求**：
- 必须从根目录开始（`/Users/mf/...`）
- 必须包含完整的多级目录层级
- 文件名必须保留标准编号和标准全称，不允许截断
- 若文件名过长，不得以"..."省略，必须写完整

**示例完整记录**：

| 字段 | 示例值 |
|---|---|
| 标准名称 | 光伏发电站设计规范 |
| 标准编号 | GB 50797-2012 |
| 分类链 | 01_发电/01_光伏发电/03_设计/01_国家标准 |
| 本地路径 | /Users/mf/Desktop/电力能源知识库/01_发电/01_光伏发电/03_设计/01_国家标准/GB_50797-2012_光伏发电站设计规范.pdf |
| 文件大小MB | 3.2 |
| 入库状态 | 新增 |

---

# 五、三大 Pipeline

## Pipeline 1：采集 Pipeline

### 目标

自动从网站发现并下载 PDF。

### 流程

```text
关键词任务
  ↓
Playwright 打开网站
  ↓
搜索标准
  ↓
识别 PDF
  ↓
下载文件
  ↓
保存 URL 与截图
```

### 推荐模式

采用：

# “任务驱动式爬取”

而不是无限制全站爬虫。

### 飞书任务表示例

| 任务 | 网站 | 关键词 | 分类 | 频率 |
|---|---|---|---|---|
| 光伏施工标准 | 学兔兔 | 光伏施工 DL/T | 发电-光伏-施工 | 每周 |
| 变电验收标准 | 学兔兔 | 变电站 验收 Q/GDW | 变电-验收 | 每月 |

---

### 反爬策略：视觉化方案

采用"浏览器视觉化方案"替代直接 DOM 取元素，降低 IP 封禁风险：

```text
Playwright 真实浏览器（Cookie 持久化）
  ↓
截图 → AI 识别页面内容 → 生成点击坐标
  ↓
模拟真实鼠标轨迹（随机偏移，非直线）
  ↓
随机等待（3~8 秒，非固定间隔）
  ↓
每下载 5~10 个文件强制休息 30秒
```
> 若出现 Error 444，需区分封禁类型：**IP 级**（换 4G 热点测试是否恢复）vs **账号级**（需新账号 + Cookie），处理方式不同，不可混淆。

---

### 关键词扩展词典

搜索前先将关键词展开为多个备用词，逐一尝试，合并去重：

```python
KEYWORD_SYNONYMS = {
    "水电":     ["水力发电", "水利发电", "水电站", "水利工程", "小水电"],
    "光伏":     ["光伏发电", "太阳能发电", "光伏电站"],
    "变电站":   ["变电所", "换流站", "开关站"],
    "输电线路": ["架空线路", "输电工程", "线路工程"],
    # 持续积累，作为项目知识资产维护
}
```

执行规则：
- 展开词逐一搜索，结果以标准编号为主键合并去重
- 搜不到的词只记录日志，不报错，继续下一个
- 词典本身作为项目资产持续积累

---

## Pipeline 2：查重 Pipeline

### 两阶段查重流程

**第一阶段：建立基准库（消化现有 PDF）**

```text
现有约 1,313 份 PDF（国家标准 + 行业标准 + 企业标准 + 电力政策）
  ↓
批量提取：文件名 + SHA256 Hash + 首页文本
  ↓
AI 解析：抽取标准编号（GB/T、DL/T、Q/GDW）
  ↓
无法识别编号 → 仅用 Hash + 标题相似度兜底
  ↓
全部写入飞书基准库
```

> 优先执行第一阶段：无需爬虫，零风险，立即可做，同时验证查重逻辑并暴露数据质量问题（命名混乱、扫描件比例等）。

**第二阶段：新爬取 PDF 对比基准库**

```text
新 PDF
  ├── 第一关：Hash 完全匹配 → "完全重复"，丢弃
  ├── 第二关：标准编号匹配 → 比较版本年份
  │         新版本 → "版本更新"，归档旧版
  │         同版本 → "重复"
  ├── 第三关：标题相似度 > 85% → "疑似重复"，人工确认
  └── 三关都过 → "新增"，进入分类流程
```

---

### 当前最大难点之一

不能仅靠文件名查重。

必须采用：

# 四层查重机制

### 第一层：标准编号查重

例如：

```text
GB/T 50796-2012
DL/T 5210
Q/GDW 1234
```

### 第二层：文件 Hash 查重

采用：

```text
SHA256
```

### 第三层：标题相似度查重

解决：

- 文件名变化
- 命名不统一

### 第四层：首页文本指纹

提取：

- PDF 首页
- 标题
- 标准号
- 目录

进行文本相似度判断。

### 输出状态

| 状态 | 含义 |
|---|---|
| 新增 | 系统无记录 |
| 已存在 | 完全重复 |
| 疑似重复 | 需人工确认 |
| 版本更新 | 新版标准 |
| 待处理 | 未分类 |

---

## Pipeline 3：分类归档 Pipeline

### 目标

自动放入正确目录。

### 流程

```text
PDF 文件
  ↓
文本提取
  ↓
AI 分类判断
  ↓
匹配目录树
  ↓
移动文件
  ↓
更新飞书
```

### 关键原则

AI 不允许自由创建分类，必须从固定枚举中选取。

## 单一数据源：directory_schema_v1.json

**所有枚举值的唯一权威来源**是项目根目录下的 `directory_schema_v1.json` 文件。（没有就创建一个）

```
三处消费方，同一个数据源：
directory_schema_v1.json
  ├── → Finder 目录结构（目录名由此生成，不允许手动创建）
  ├── → 飞书多维表格下拉菜单（板块/项目/阶段/标准类型四个字段的枚举值）
  └── → AI Classify Agent（分类判断时从此读取，不允许超出枚举范围）
```

**修改流程**：任何枚举调整，必须先修改 `directory_schema_v1.json`，由脚本同步到 Finder 目录和飞书字段，禁止直接在飞书或 Finder 中手动新建分类。

### directory_schema_v1.json（完整内容）

```json
{
  "_comment": "单一数据源。修改此文件后运行 sync_schema.py 同步到 Finder 和飞书。",
  "_version": "1.0",
  "_updated": "2026-05-08",

  "root": [
    "发电",
    "输电",
    "变电",
    "配电",
    "用电"
  ],

  "project": {
    "发电": [
      "光伏发电",
      "风力发电",
      "火力发电",
      "水力发电",
      "垃圾发电",
      "氢气发电",
      "核电"
    ],
    "输电": [],
    "变电": [],
    "配电": [],
    "用电": []
  },

  "lifecycle": [
    "通用",
    "勘察",
    "设计",
    "造价",
    "施工验收",
    "运维",
    "安全"
  ],

  "standard_type": [
    "国家标准",
    "行业标准",
    "地方标准",
    "企业标准"
  ]
}
```

### 目录路径生成规则

由 JSON Schema 推导出 Finder 目录路径，规则如下：

```
电力能源知识库/
└── {root}/
    └── {project}/          ← 若 project 为空（如输电），直接到 lifecycle
        └── {lifecycle}/
            └── {standard_type}/
                └── {标准编号}_{标准名称}.pdf
```

**示例**：

```
电力能源知识库/
├── 发电/
│   ├── 光伏发电/
│   │   ├── 通用/
│   │   │   ├── 国家标准/
│   │   │   ├── 行业标准/
│   │   │   ├── 地方标准/
│   │   │   └── 企业标准/
│   │   ├── 勘察/
│   │   ├── 设计/
│   │   ├── 造价/
│   │   ├── 施工验收/
│   │   ├── 运维/
│   │   └── 安全/
│   └── 风力发电/
│       └── ...
├── 输电/
│   ├── 通用/
│   │   ├── 国家标准/
│   │   └── ...
│   └── ...
└── ...
```

### 飞书下拉菜单与 JSON 的对应关系

| 飞书字段 | JSON 键 | 下拉选项来源 |
|---|---|---|
| 板块 | `root` | 发电 / 输电 / 变电 / 配电 / 用电 |
| 项目 | `project[板块]` | 由"板块"联动，如板块=发电则显示光伏/风力/... |
| 阶段 | `lifecycle` | 通用 / 勘察 / 设计 / 造价 / 施工验收 / 运维 / 安全 |
| 标准类型 | `standard_type` | 国家标准 / 行业标准 / 地方标准 / 企业标准 |

"分类链"字段由以上四个字段自动拼接（`板块/项目/阶段/标准类型`），与本地 Finder 路径后四级完全一致，实现"横着看竖着看都一样"。

---

# 六、本地工作区设计

本地工作区分为两个部分，均放在戴总电脑（`/Users/mf/Desktop/`）上：

```
/Users/mf/Desktop/
├── Power_KB_Automation/    ← AI 运行时的流水线工作区（文件在此流转，不长期存储）
└── 电力能源知识库/          ← 最终归档区（长期存放，与飞书多维表格一一对应）
```

---

## 第一部分：Pipeline 流水线工作区

**用一句话说明**：这是 AI 处理文件时的"中转站"，文件下载后在这里经过查重、分类，最终移入知识库，不在这里长期存放。

```text
Power_KB_Automation/
├── 00_Inbox_待处理/       ← 新下载的 PDF 先放这里，还没有处理
├── 01_Dedup_查重中/       ← 正在被查重判断（临时目录，处理完即清空）
├── 02_Classified_已分类/  ← 查重通过、分类完成，等待移入知识库
├── 03_Review_待人工确认/  ← AI 判断不了的文件放这里，需要你来看一眼
├── 05_Logs_运行日志/      ← 每次运行的记录（哪些成功、哪些失败）
├── 06_Screenshots_截图/   ← AI 操作浏览器时的截图（出问题可以查）
└── 07_Reports_周报月报/   ← 自动生成的工作汇报
```

**文件在流水线中的完整流转顺序**：

```text
下载完成
  ↓
00_Inbox_待处理/
  ↓ Dedup Agent 查重
  ├── 完全重复 → 飞书标注"重复"，直接删除（不占磁盘）
  ├── 疑似重复 → 03_Review_待人工确认/（你来判断要不要留）
  └── 新增 / 版本更新 → 02_Classified_已分类/
                          ↓ Classify Agent 分类
                          ├── 能判断分类 → 飞书写入目标路径，文件等待移动
                          └── 判断不了  → 03_Review_待人工确认/
                          ↓ Index Agent 归档
                          └── 移入电力能源知识库/{01_发电}/{01_光伏发电}/...
                              飞书记录同步完成 ✅
```

> `03_Review_待人工确认/` 是你唯一需要主动处理的目录：里面的文件 AI 无法自动决策，需要你判断是保留还是丢弃，并手动归类。

---

## 第二部分：电力能源知识库（最终归档区）

目录结构由 `directory_schema_v1.json` 生成，命名统一使用 `序号_名称` 格式：

```text
/Users/mf/Desktop/电力能源知识库/
├── 01_发电/
│   ├── 01_光伏发电/
│   ├── 01_光伏发电/
│   │   ├── 01_通用/
│   │   │   ├── 01_国家标准/
│   │   │   ├── 02_行业标准/
│   │   │   ├── 03_地方标准/
│   │   │   └── 04_企业标准/
│   │   ├── 02_勘察/
│   │   ├── 03_设计/
│   │   │   ├── 01_国家标准/
│   │   │   │   └── GB_50797-2012_光伏发电站设计规范.pdf
│   │   │   ├── 02_行业标准/
│   │   │   ├── 03_地方标准/
│   │   │   │   └── DB11_T_1671-2019_北京市光伏xxx.pdf
│   │   │   └── 04_企业标准/
│   │   ├── 04_造价/
│   │   ├── 05_施工验收/
│   │   ├── 06_运维/
│   │   └── 07_安全/
│   ├── 02_风力发电/
│   └── ...（其他发电项目同结构）
├── 02_输电/
│   ├── 01_通用/
│   │   ├── 01_国家标准/
│   │   ├── 02_行业标准/
│   │   ├── 03_地方标准/
│   │   └── 04_企业标准/
│   ├── 02_勘察/
│   ├── 03_设计/
│   ├── 04_造价/
│   ├── 05_施工验收/
│   └── ...（其他阶段同结构）
├── 03_变电/    （同输电结构）
├── 04_配电/    （同输电结构）
└── 05_用电/    （同输电结构）
```

---

## 目录与飞书字段的对应关系

飞书每一行记录 ↔ 本地一个 PDF 文件，路径可互相还原：

| 飞书字段 | 对应目录层级 | 示例值 |
|---|---|---|
| 板块 | 第1层目录 | `01_发电` |
| 项目 | 第2层目录 | `01_光伏发电` |
| 阶段 | 第3层目录 | `03_设计` |
| 标准类型 | 第4层目录 | `03_地方标准` |
| 本地路径（完整） | 根目录到文件名 | `/Users/mf/Desktop/电力能源知识库/01_发电/01_光伏发电/03_设计/03_地方标准/DB11_T_1671-2019_xxx.pdf` |

> **一致性保证**：Index Agent 在每批次结束后自动校验飞书记录的本地路径是否实际存在，不存在则取消"文件存在"勾选并发出告警。

---

# 七、核心数据闭环

每个 PDF 必须形成：

```text
来源 URL
  ↓
下载文件
  ↓
文件 Hash
  ↓
标准编号
  ↓
业务分类
  ↓
Finder 路径
  ↓
飞书记录
  ↓
批次号
  ↓
小程序导入状态
```

---

# 八、Agent 架构

建议拆分多个 Agent，不要一个 Agent 全干。每个 Agent 职责单一、可独立运行、可被主控 Agent 调度。

---

## Agent 分工详细说明

### 1. Search Agent（搜索 Agent）

**职责**：在指定网站上按关键词搜索标准，收集候选 PDF 的页面信息，一定要是可以下载的pdf直链，而不是空的，异常的

**具体行为**：
1. 先正确的理解需要搜索的网站，以及方式，和注意事项，会使用VIP账号的登入方法论
2. 调用关键词扩展词典，将本地的系统文件夹架构的关键词展开为备用词列表（可以进行沟通）
3. 启动 Playwright 浏览器，加载已有 Cookie，打开目标网站
4. 对每个备用词依次执行搜索，截图确认页面加载完成
5. 识别搜索结果页的总页数，写入任务状态对象（`total_pages`）
6. 逐页截图，用 AI 识别每条搜索结果的标题、标准编号、文件链接
7. 每处理完一页更新 `pages_processed` 计数
8. 所有备用词搜索完成后，对结果以标准编号为主键去重
9. 将候选列表写入飞书"待下载队列"，标注来源关键词和来源页面 URL
10. 搜不到的备用词只记录日志，不报错

**输出**：候选 PDF 列表（标题 + 标准编号 + 下载页 URL）

**完成条件**：`pages_processed == total_pages` 且所有备用词均已尝试

---

### 2. Download Agent（下载 Agent）

**职责**：从网站下载 PDF 文件到本地 Inbox 目录。

**具体行为**：
1. 从飞书"待下载队列"读取未下载的条目
2. 逐条打开下载页 URL，截图确认页面状态
3. 用 AI 识别下载按钮坐标，模拟真实鼠标点击（随机偏移，非直线轨迹）
4. 等待文件下载完成（随机等待 3~8 秒，非固定间隔）
5. 每下载 5~10 个文件强制休息 30~120 秒，模拟人工操作节奏
6. 下载完成后将文件移入 `00_Inbox_待处理/` 目录
7. 记录：原始文件名、下载时间、来源 URL、文件大小、截图路径
8. 若下载失败（超时/404/Error 444），记录失败原因，标记为"下载失败"，不重试，由主控 Agent 决定后续处理
9. 将下载结果（成功/失败）更新回飞书队列

**输出**：PDF 文件落地到 `00_Inbox_待处理/`，附带元数据记录

**完成条件**：队列中所有条目均已处理（成功或失败均需标记）

---

### 3. Dedup Agent（查重 Agent）

**职责**：对 Inbox 中的新 PDF 进行四层查重，判断是否重复。

**具体行为**：
1. 扫描 `00_Inbox_待处理/` 目录中的新文件
2. 对每个文件执行四层查重：
   - **第一层**：计算 SHA256 Hash，与飞书基准库比对 → 完全匹配则判定"完全重复"
   - **第二层**：用 pdfplumber 提取首页文本，用正则解析标准编号（GB/T、DL/T、Q/GDW）→ 与基准库标准编号比对，命中则比较版本年份
   - **第三层**：提取标准名称，与基准库做标题相似度计算（阈值 85%）→ 超过阈值判定"疑似重复"
   - **第四层**：提取首页文本指纹（标题 + 目录前三条），做文本相似度判断
3. 输出查重状态：新增 / 完全重复 / 版本更新 / 疑似重复 / 待处理
4. 将查重结果写入飞书，更新"入库状态"字段
5. "完全重复"文件：飞书标注状态后**直接删除本地文件**，不占磁盘，不进入后续流程
6. "疑似重复"文件移入 `03_Review_待人工确认/`，等待人工处理
7. "版本更新"文件移入 `02_Classified_已分类/`，同时将飞书中旧版标记为"已废止"
8. "新增"文件移入 `02_Classified_已分类/`，等待 Classify Agent 处理

**输出**：每个文件的查重状态 + 飞书字段更新

**完成条件**：Inbox 中所有文件均已完成查重判定

---

### 4. Classify Agent（分类 Agent）

**职责**：对通过查重的新 PDF 进行业务分类，生成目标归档路径。

**具体行为**：
1. 读取状态为"新增"或"版本更新"的待分类文件
2. 提取 PDF 首页文本（标准编号、标准名称、适用范围、关键词）
3. 将提取内容发送给 AI，要求从固定枚举中选取分类，不允许自由创建：
   - 流程分类（板块）：发电 / 输电 / 变电 / 配电 / 用电（必选一）
   - 项目分类：光伏发电 / 风力发电 / 火力发电 / 水力发电 / 垃圾发电 / 氢气发电 / 核电（无项目的板块可为空）
   - 阶段分类：通用 / 勘察 / 设计 / 造价 / 施工验收 / 运维 / 安全（必选一）
   - 标准类型：国家标准 / 行业标准 / 地方标准 / 企业标准（必选一）
4. AI 返回分类结果后，拼接为完整目标路径（绝对路径）
5. 确认目标目录在 `directory_schema_v1.json` 中存在，不允许新建未经审批的分类
6. 若 AI 无法判断分类，将文件移入 `03_Review_待人工确认/`，不强行分类
7. 将分类结果（分类链 + 目标绝对路径）更新到飞书，标记"待归档"

**输出**：每个文件的目标绝对路径 + 飞书字段更新

**完成条件**：所有待分类文件均已生成目标路径或移入人工确认区

---

### 5. Index Agent（飞书写入 Agent）

**职责**：将归档完成的 PDF 信息完整写入飞书多维表格，形成可检索的索引。

**具体行为**：
1. 监听分类归档完成的文件列表
2. 对每个文件，将文件实际移动到目标绝对路径（执行 Finder 归档操作）
3. 归档成功后，在飞书多维表格中创建或更新记录，写入以下字段：
   - ID（自动编号）
   - 标准名称、标准编号
   - 板块、分类链
   - 本地路径（完整绝对路径，精确到文件名）
   - 文件大小MB、文件Hash
   - 来源URL、下载时间
   - 批次号、入库状态
4. 更新"文件存在"复选框（归档成功 → 勾选）
5. 若文件移动失败（磁盘满/权限等），记录错误，不写入飞书，由主控 Agent 告警
6. 定期（每批次结束后）执行一致性校验：遍历飞书记录，检查本地路径文件是否实际存在，不存在则取消"文件存在"勾选并告警

**输出**：飞书多维表格完整记录 + 本地 Finder 归档完成

**完成条件**：所有待归档文件均已写入飞书且本地路径可验证

---

### 6. Report Agent（周报 Agent）

**职责**：在每个批次结束后，自动生成工作汇报。

**具体行为**：
1. 从飞书读取本批次的所有记录（按批次号筛选）
2. 统计本批次数据：
   - 新增文件数 / 总文件数
   - 重复文件数 / 版本更新数 / 疑似重复数（待人工处理）
   - 各分类下的新增数量分布
   - 来源网站分布
   - 下载失败数量及原因汇总
3. 生成 Markdown 格式周报，存入 `07_Reports_周报月报/`
4. 将周报摘要发送到飞书群聊（指定通知群）
5. 若本批次存在"待人工确认"文件，在周报中单独列出，方便人工处理

**输出**：`07_Reports_周报月报/批次报告_YYYYMMDD.md` + 飞书消息通知

**完成条件**：报告生成并发送完毕

---

## 主控 Agent：状态机监督模式

不干活，只负责**发布任务 → 检查完成状态 → 验收结果 → 告警异常**。

每个搜索任务维护一个状态对象：

```json
{
  "task_id": "光伏施工标准_学兔兔_20260508",
  "keyword": "光伏施工",
  "total_pages": 8,
  "pages_processed": 3,
  "items_found": 27,
  "status": "in_progress",
  "last_checkpoint": "page_3"
}
```

**强制完成条件**：`pages_processed == total_pages` 才允许标记完成，防止 AI 只处理部分页面就自行结束任务。

每日汇报内容：

```text
今天做什么？
哪些网站要查？
发现多少新标准？
哪些重复？
哪些需要人工确认？
本周交付什么？
```

---

# 九、推荐技术栈

| 模块 | 技术 |
|---|---|
| 浏览器自动化 | Playwright |
| 浏览器管理 | HTTP API |
| AI 分类 | Claude / Gemini |
| 本地目录管理 | Python |
| PDF 解析 | pdfplumber / pymupdf |
| Hash | SHA256 |
| 飞书同步 | 飞书开放平台 API |
| 调度 | cron / LaunchAgent |
| 日志 | SQLite / JSON |
| 周报生成 | Markdown |

---

# 十、MVP 最小可行版本

## 第一阶段不要做全自动。

先跑通：

```text
搜索
→ 下载
→ 查重
→ 分类
→ 飞书写入
→ Finder归档
→ 周报生成
```

### 推荐第一阶段规模

```text
50份 PDF
```

验证：

- 分类逻辑
- 查重稳定性
- 飞书同步
- Finder结构
- 目录体系

---

# 十一、执行优先级（更新版）

## ✅ 第一优先级：建立飞书基准索引（已完成，2026-05-08）

建立 1,313 条记录的飞书基准库，覆盖国家标准 + 行业标准 + 企业标准 + 电力政策全4类：

```text
Excel（标准规范引用名录一览表1.14.xlsx）
  ↓ 批量解析（4个Sheet，列分组结构）
  ↓ lark-cli api POST batch_create（每批100条，约25秒完成）
  ↓ 飞书表 第0批次_知识库标准目录（tbl3lbvONB2cfOhI）
  ↓ 校验：飞书总数 = Excel总数 = 1,313 ✅
```

> 实际执行时间：约 5 分钟完成全部建表、导入、清理、校验工作。详细执行参考见第十六章。

## ✅ 第二优先级：飞书多维表格字段设计（已完成，2026-05-08）

按本文档"四·2"字段规范完成建表，13个字段全部到位，阶段枚举采用 Method A（与Excel列组对齐），仪表盘5张图表已创建并验证可下钻。

## 第三优先级：Finder 目录树标准化 + 锁定（待执行）

生成 `directory_schema_v1.json`，后续批次只能在已有分类下新增文件，禁止新建一级/二级分类（需人工审批）。

> 前置动作：先对 2.4GB 本地 PDF 做一次文件名清理和目录预建，再锁定 schema。

## 第四优先级：查重模块（待执行）

在 Hash 基准库建立完毕后（需对本地2.4GB PDF批量提取SHA256），实现：

```text
标准编号匹配 + 文件Hash完全匹配（两层即可覆盖90%+的重复判断）
```

## 第五优先级：自动下载（视觉化方案）（待执行）

在查重逻辑稳定后，再开始爬取新内容。

---

# 十二、最终目标形态

最终系统：

```text
AI 定时运行
  ↓
自动搜索网站
  ↓
发现新标准
  ↓
自动下载
  ↓
自动查重
  ↓
自动分类
  ↓
自动写入飞书
  ↓
自动生成交付报告
  ↓
等待小程序导入
```

---

# 十三、核心理念总结

## Finder 管：

# “书架”

## 飞书 管：

# “索引”

## AI 管：

# “流程”

## 人工只负责：

# “异常处理”

---

# 十四、后续需要深入讨论的技术问题

后续重点：

1. PDF 元数据标准化
2. 标准编号解析
3. 多版本标准升级策略
4. OCR 与扫描件识别
5. 企业标准命名混乱问题
6. 大规模目录扩展
7. 增量同步机制
8. AI 分类准确率评估
9. 飞书与本地目录一致性校验
10. 自动周报/月报体系
11. Agent 调度与容错
12. Token 成本优化
13. 大规模知识库 Embedding 策略
14. 小程序导入格式标准化

---

# 十五、项目本质

这个系统本质上不是：

# “PDF 下载器”

而是：

# “行业知识资产流水线”

目标不是：

“收集文件”

而是：

# “持续生产可管理、可追踪、可交付的行业知识资产”

---

# 十六、AI 执行技术参考手册

> **本章专为 AI Agent 设计**。所有 ID、命令、代码均经过实际验证（2026-05-08）。AI 执行任何飞书操作前，必须先查阅本章，不得凭记忆或猜测执行。

---

## 飞书 Base 关键 ID

| 资源 | ID | 说明 |
|---|---|---|
| Base Token | `G0sMbCDFVag3ensKokLc93DAnsd` | 整个多维表格的根入口 |
| 第0批次索引表 | `tbl3lbvONB2cfOhI` | `第0批次_知识库标准目录`，1,313条，主工作表 |
| 旧历史表 | `tblzKjEySPkSCsWR` | `知识库PDF清单分类`，1,204条，**只读，禁止修改** |
| 仪表盘 | `blkXy9bXkbysKArc` | `第0批次_知识库统计`，5张图表 |

## 第0批次索引表字段 ID 对照

| 字段名 | 字段 ID | 类型 | 可写 |
|---|---|---|---|
| ID（自动编号） | `fldzrLwvoM` | 自动编号 | 只读 |
| 标准名称 | `fld0ITWeoC` | 文本 | 可写 |
| 标准类型 | `fld8PY5Mce` | 单选 | 可写 |
| 标准编号 | `fld8tY9Kpo` | 文本 | 可写 |
| 分类链 | `fldaSpVIxu` | 文本 | 可写 |
| 阶段 | `fldbGCHDIb` | 单选 | 可写 |
| 备注 | `fldDdIoGqd` | 文本 | 可写 |
| 文件大小MB | `fldEa1PnGK` | 数字 | 可写 |
| 项目 | `fldeEItP4F` | 单选 | 可写 |
| 文件存在 | `fldhMQirPJ` | 复选框 | 可写 |
| 批次 | `fldKAXr3lo` | 文本 | 可写 |
| 板块 | `fldmDg206J` | 单选 | 可写 |
| 本地路径 | `fldNPaaaXD` | 文本 | 可写 |
| 入库状态 | `fldrcmkHh1` | 单选 | 可写 |

---

## 飞书写入规范：必须使用 batch_create

**禁止**使用 `lark-cli base +record-upsert` 逐条写入（约0.3秒/条，1000条需6-7分钟）。

**必须**使用 `lark-cli api POST` 调用 batch_create 批量写入（每批100条，1000条约8秒）。

### 批量写入模板（Python）

```python
import subprocess, json

BASE_TOKEN = "G0sMbCDFVag3ensKokLc93DAnsd"
TABLE_ID   = "tbl3lbvONB2cfOhI"
BATCH_URL  = f"/open-apis/bitable/v1/apps/{BASE_TOKEN}/tables/{TABLE_ID}/records/batch_create"

def batch_create(records: list, batch_size=100):
    for i in range(0, len(records), batch_size):
        batch = records[i:i+batch_size]
        payload = json.dumps(
            {"records": [{"fields": r} for r in batch]},
            ensure_ascii=False
        )
        result = subprocess.run(
            ["lark-cli", "api", "POST", BATCH_URL, "--data", payload],
            capture_output=True, text=True
        )
        resp = json.loads(result.stdout)
        if resp.get("code") != 0:
            raise RuntimeError(f"Batch {i//batch_size+1} failed: {resp.get('msg')}")
        print(f"Batch {i//batch_size+1}: +{len(batch)} records")
```

### 批量删除模板（Python）

```python
DEL_URL = f"/open-apis/bitable/v1/apps/{BASE_TOKEN}/tables/{TABLE_ID}/records/batch_delete"

def batch_delete(record_ids: list):
    for i in range(0, len(record_ids), 500):
        batch = record_ids[i:i+500]
        payload = json.dumps({"records": batch}, ensure_ascii=False)
        result = subprocess.run(
            ["lark-cli", "api", "POST", DEL_URL, "--data", payload],
            capture_output=True, text=True
        )
        resp = json.loads(result.stdout)
        if resp.get("code") != 0:
            raise RuntimeError(f"Delete batch failed: {resp.get('msg')}")
```

### 获取 record_id 的正确方式

```python
# +record-list 返回结构中，record_id_list 与 data 数组同序一一对应
result = subprocess.run(
    ["lark-cli", "base", "+record-list",
     "--base-token", BASE_TOKEN, "--table-id", TABLE_ID,
     "--limit", "200", "--offset", "0"],
    capture_output=True, text=True
)
d = json.loads(result.stdout)["data"]
record_ids = d["record_id_list"]   # 真实删除用的 ID，与 data 同序
field_ids  = d["field_id_list"]    # 字段 ID 顺序
rows       = d["data"]             # rows[i][j] 对应 field_ids[j]
```

---

## Excel 解析规范（标准规范引用名录格式）

该 Excel 采用"列分组"布局，与普通纵向表格不同，**不能直接 pd.read_excel 读取**。

### 固定行结构

| 行索引 | 内容 | 处理方式 |
|---|---|---|
| Row 0（index 0） | 大标题（如"国家标准"） | 跳过 |
| Row 1（index 1） | 空行 | 跳过 |
| Row 2（index 2） | 分组列头（如"通用基础标准"、"设计标准"） | 解析为 stage 映射 |
| Row 3（index 3） | 子列头（序号 / 标准名称 交替） | 跳过 |
| Row 4+（index 4~） | 实际数据行 | 逐行解析 |

### 列布局规则

- 每2列一组：偶数列 = 分组头（stage），奇数列 = 标准名称
- 分组头只出现在 Row 2 的偶数列（0, 2, 4, 6, 8, 10...）
- 数据在奇数列（col 1, 3, 5, 7, 9, 11...）

### 解析代码模式（Python/openpyxl）

```python
import openpyxl

STAGE_MAP = {
    "通用基础标准":    "通用",
    "勘察标准":       "勘察",
    "设计标准":       "设计",
    "造价标准":       "造价",
    "施工、验收标准": "施工验收",
    "运维、规范标准": "运维",
}

TYPE_MAP = {  # Sheet名 -> 标准类型枚举值
    "国家标准": "国家标准",
    "行业标准": "行业标准",
    "企业标准": "企业标准",
    "电力政策": "行业标准",  # 政策文件归入行业标准类
}

def parse_sheet(ws, std_type: str) -> list:
    rows = list(ws.iter_rows(values_only=True))
    group_row = rows[2]

    col_to_stage = {
        ci + 1: STAGE_MAP.get(str(group_row[ci]).strip(), str(group_row[ci]).strip())
        for ci in range(0, len(group_row), 2)
        if ci < len(group_row) and group_row[ci] and str(group_row[ci]).strip()
    }

    records = []
    for row in rows[4:]:
        for name_col, stage in col_to_stage.items():
            if name_col >= len(row):
                continue
            name = row[name_col]
            if not name or not str(name).strip():
                continue
            name_str = str(name).strip()
            if name_str in ("标准名称", "序号", "标 准名称"):
                continue
            records.append({
                "标准名称": name_str,
                "阶段":     stage,
                "标准类型": std_type,
                "批次":     "第0批次",
                "入库状态": "已导入",
            })
    return records
```

> **政策文件特殊处理**：电力政策 Sheet 没有列分组结构，数据直接在固定列，阶段字段留空，分类名放入备注字段。

---

## 仪表盘配置规范

### group_by 的 mode 选择规则

| 字段类型 | mode 值 | 原因 |
|---|---|---|
| 单选（板块/阶段/标准类型/入库状态） | integrated | 每条记录只有一个值，按值聚合 |
| 多选、人员等多值字段 | enumerated | 每个选项拆开独立统计 |

### 5张图表的创建参数

| 图表名 | type | group_by 字段 | 说明 |
|---|---|---|---|
| 总数指标卡 | statistics | 无，count_all=true | 显示总记录数 |
| 标准类型环形图 | ring | 标准类型 | 国标/行标/企标/政策占比 |
| 阶段条形图 | bar | 阶段 | 各工程阶段数量 |
| 阶段×类型交叉 | column | 阶段（主）+ 标准类型（副） | 堆叠柱状图 |
| 入库状态饼图 | pie | 入库状态 | 已导入/待处理等状态分布 |

```bash
# 创建仪表盘
lark-cli base +dashboard-create \
  --base-token "G0sMbCDFVag3ensKokLc93DAnsd" \
  --name "第X批次_知识库统计" \
  --theme-style "SimpleBlue"

# 创建阶段条形图（示例）
lark-cli base +dashboard-block-create \
  --base-token "G0sMbCDFVag3ensKokLc93DAnsd" \
  --dashboard-id "<dashboard_id>" \
  --name "按工程阶段分布" \
  --type "bar" \
  --data-config '{"table_name":"第0批次_知识库标准目录","count_all":true,"group_by":[{"field_name":"阶段","mode":"integrated","sort":{"type":"value","order":"desc"}}]}'
```

---

## 数据校验 SOP

每次批量导入后，必须执行以下验证：

```python
# 1. 统计飞书表实际记录数
total, offset = 0, 0
while True:
    r = subprocess.run(
        ["lark-cli", "base", "+record-list",
         "--base-token", BASE_TOKEN, "--table-id", TABLE_ID,
         "--limit", "200", "--offset", str(offset)],
        capture_output=True, text=True
    )
    cnt = len(json.loads(r.stdout)["data"]["data"])
    total += cnt
    if cnt < 200:
        break
    offset += 200

# 2. 统计 Excel 原始记录数
import openpyxl
wb = openpyxl.load_workbook("标准规范引用名录一览表1.14.xlsx")
excel_total = sum(len(parse_sheet(wb[s], TYPE_MAP[s])) for s in TYPE_MAP if s in wb.sheetnames)

# 3. 断言
assert total == excel_total, f"数量不符：飞书={total}, Excel={excel_total}"
print(f"校验通过：{total} 条记录完全一致")
```

---

## 旧记录识别与清理规范

如遇到重复导入，用以下特征识别旧/错误记录：

| 特征 | 识别方式 | 处理 |
|---|---|---|
| 备注字段含"来源Sheet" | "来源Sheet" in record[备注列] | 批量删除 |
| 标准名称为测试值 | name in ("test_batch", "test") | 批量删除 |
| 自动编号格式异常 | 手动检查 | 人工确认后删除 |

清理后必须重新执行数据校验 SOP，确认总数与 Excel 一致。

---

## 后续批次执行指引（V0.2 修订）

> **⚠️ V0.2 修订说明（2026-05-09）**：V0.1 原文表述为"新建表，不要往旧表追加；每个批次表独立存在"。经唐总 2026-05-09 明确：
>
> **正确纪律 = 不同批次先独立查重，再统一装到一个表里。**
>
> 即：每个批次的**原始数据**（Excel 导入 / 爬虫抓取 / 存量 PDF 登记）先独立清洗和查重，确认无冲突后，**全部合并进同一张主索引表**（当前主表：`第0批次_知识库标准目录` / `tbl3lbvONB2cfOhI`），通过 `批次` 字段区分来源。
>
> 目的：只有一张主索引表可查询，便于 Dashboard、小程序、AI 检索统一消费。下方流程据此更新。

**新批次入库顺序**：

1. **准备原始数据**：爬虫产出 / Excel 导入 / 存量 PDF 扫描，落盘到临时目录（不直接写飞书）
2. **字段对齐**：按第0批次 14 字段 schema 做字段映射，缺枚举值先扩枚举（见本章「枚举扩展规范」）
3. **本批次内查重**：对本批次原始数据做四层查重（Hash + 标准编号 + 标题 + 首页文本），去除批内重复
4. **与主表查重**：本批次清洗后数据 vs 主表现有记录做四层查重，标注"新增 / 完全重复 / 版本更新 / 疑似重复"
5. **批量灌入主表**：只灌"新增"和"版本更新"记录，`批次` 字段填本次批号（如 `4.22标准` / `政策监控` / `202605` 等）
6. **校验**：主表新增记录数 = 本批次"新增+版本更新"数，必须一致
7. **旧独立表归档**：若此前已存在独立批次表（如 `知识库PDF清单分类` / `tblzKjEySPkSCsWR`），数据迁入主表后**保持只读**，不删不改，作为历史回溯源

**旧表**（`tblzKjEySPkSCsWR`）**迁移执行 SOP**：见本文档第十七章。

## 枚举扩展规范

任何新批次引入的新枚举值，必须按以下流程走：

1. **先扫描本批次原始数据**，统计所有出现但主表枚举里没有的值
2. **清单先交给决策人（唐总）确认**是否收录、合并、改写
3. **审批通过后**，用 `lark-cli base +field-update --json '{"field_name":"...","type":"select","options":[...]}'` 统一扩展（注意 type 用字面字符串 `select`，`options` 必须平铺在顶层，不要嵌入 `property`）
4. **同步写回** `directory_schema_v1.json`，保持单一数据源
5. **留痕**：每次扩展写入 `schema_changelog.md`（时间 + 字段 + 新增值 + 审批人）

## 工作脚本位置

### 第0批次（2026-05-08 已完工）

| 脚本 | 路径 | 用途 |
|---|---|---|
| 导入脚本 | `/Users/tang/Desktop/import_batch.py` | Excel→飞书批量导入 |
| 清理脚本 | `/Users/tang/Desktop/cleanup_feishu.py` | 删除重复旧记录 |
| 源数据 | `/Users/tang/Desktop/标准规范引用名录一览表1.14.xlsx` | 1,313条原始数据 |

### 旧表迁移（2026-05-09 启动，执行端：小琳 OpenClaw workspace）

| 脚本/产物 | 路径 | 用途 |
|---|---|---|
| Dump 脚本 | `~/.openclaw/workspace/migration/01_dump_old_table.py` | 拉全旧表 1,204 条 |
| 原始快照 | `~/.openclaw/workspace/migration/raw_old_table.jsonl` | 每行一条旧表记录 |
| 字段映射规则 | `~/.openclaw/workspace/migration/mapping_rules.json` | 板块/阶段/状态/项目映射表 |
| 映射后数据 | `~/.openclaw/workspace/migration/mapped_records.jsonl` | 待灌入主表的记录 |
| 映射审阅 CSV | `~/.openclaw/workspace/migration/02_mapping.csv` | 人肉审阅用 |
| 试点日志 | `~/.openclaw/workspace/migration/pilot_log.jsonl` | 20 条试点灌表结果 |
| 批量日志 | `~/.openclaw/workspace/migration/import_log.jsonl` | 全量灌表结果 |
| 失败记录 | `~/.openclaw/workspace/migration/failed_records.jsonl` | 灌表失败留痕 |
| 校验报告 | `~/.openclaw/workspace/migration/verification_report.md` | 三重校验结果 |
| 最终交付单 | `~/.openclaw/workspace/migration/DONE.md` | 唐总签字确认 |
| 架构文档 | `~/.openclaw/workspace/kb-pipeline-doc/电力能源行业知识库自动化_Pipeline架构方案_V0.1.md` | 本文档（GitHub 镜像）|

---

# 十七、当前执行任务：旧表迁移入主表（MAU SOP）

> **本章是任何 AI 接手旧表迁移任务时的操作手册。** 所有 MAU（Minimum Action Unit，最小行动单元）必须按顺序执行，每个单元都有**输入 / 动作 / 产出 / 验收 / 留痕**五段。遇 ⛔ 卡点必须停下等决策人（唐总）签字。

## 17.0 任务总览

| 项目 | 值 |
|---|---|
| 任务名 | 旧表 `知识库PDF清单分类`（1,204 条）→ 主表 `第0批次_知识库标准目录` |
| 源表 ID | `tblzKjEySPkSCsWR`（只读，不动） |
| 目标表 ID | `tbl3lbvONB2cfOhI` |
| Base Token | `G0sMbCDFVag3ensKokLc93DAnsd` |
| 最终预期记录数 | 2,517（1,313 + 1,204） |
| 当前进度 | MAU-0 ✅ 枚举已扩3项；MAU-1~12 待执行 |
| 执行人 | 小琳（OpenClaw agent）+ 子代理 |
| 决策人 | 唐泽龙（唐总） |
| 工期估算 | 1-2 天 |

## 17.1 迁移字段规则（唐总 2026-05-09 06:29 定稿）

| 旧表字段 | 主表字段 | 规则 |
|---|---|---|
| 标准名称 | 标准名称 | 原值迁移 |
| 标准编号 | 标准编号 | 原值迁移 |
| 板块（带序号） | 板块（单选） | `1、发电`→`发电`、`3、变电`→`变电` 等；`通用标准` / `政策监控` 保留为独立板块 |
| 项目 | 项目（单选） | 原值迁移；缺失枚举值**扩枚举** |
| 阶段（带序号） | 阶段（单选） | `5、运维`→`运维`、`1、勘测设计`→`勘测设计`（已扩）等 |
| 标准类型 | 标准类型（单选） | 旧表全空，按命名规则推断：`GB_T_`→国家标准，`DL_T_`→行业标准，`Q_GDW_`→企业标准，政策文件→政策文件；无法推断留空 |
| 分类链（二级） | 分类链（文本） | 按新规则拼接：`板块/项目/阶段/标准类型`（四级），缺项用空串 |
| 本地路径 | 本地路径 | **必带**（第0批次本地路径全空，旧表能补上） |
| 备注 | 备注 | **必带**（如"4.22批次第X条" / 来源说明） |
| 文件大小MB | 文件大小MB | 原值迁移 |
| 文件存在 | 文件存在 | 以 `os.path.exists(本地路径)` 实测为准 |
| 入库状态 | 入库状态（单选） | **保留原值**：`已爬取PDF` / `已入戴总库` / `已交付PDF` / `已交付韩轩` / `待爬取PDF`（已扩枚举 ✅） |
| — | 批次 | 按旧表 `批次` 字段值填入（如 `4.22标准` / `政策监控`），不改写 |

## 17.2 已完成的枚举扩展（MAU-0，2026-05-09 06:35）

| 字段 | 新增枚举值 | 方式 |
|---|---|---|
| 入库状态 | `已爬取PDF` / `已入戴总库` / `已交付PDF` / `已交付韩轩` / `待爬取PDF` | `lark-cli base +field-update` ✅ |
| 板块 | `通用标准` / `政策监控` | 同上 ✅ |
| 阶段 | `勘测设计` | 同上 ✅ |
| 项目 | **待扩**（需 MAU-3 扫描后统计） | ⏳ |
| 标准类型 | **待扩**（政策文件已在，其他按旧表扫描后确认） | ⏳ |

## 17.3 MAU 执行清单（12 个单元 · 4 阶段）

### 阶段一：数据下钻（子代理执行，预计 1 小时）

#### MAU-1 · 旧表全量 dump
- **输入**：`tblzKjEySPkSCsWR`，Base Token，已装的 `lark-cli`
- **动作**：
  1. 调用 `lark-cli base +record-list --limit 200 --offset N` 循环拉取，或 MCP `feishu_bitable_list_records` 分页至 has_more=false
  2. 每条记录保留 record_id + 完整 fields
- **产出**：`migration/raw_old_table.jsonl`（每行一条 JSON）
- **验收**：行数 == 1204；无重复 record_id；文件可 `jq -c . | wc -l` 校验
- **留痕**：完成后 `log.md` 追加 `[时间] MAU-1 完成，拉取 1204 条`

#### MAU-2 · 枚举实测扫描（项目 + 标准类型）
- **输入**：`migration/raw_old_table.jsonl`
- **动作**：
  1. 遍历所有记录，统计 `项目` 字段所有非空唯一值 → 清单 A
  2. 遍历 `本地路径` + `标准编号`，按规则推断 `标准类型` → 清单 B
  3. 对比主表现有枚举，列出缺口
- **产出**：`migration/enum_gaps.md`（两个缺口清单 + 每个值的样例记录）
- **验收**：清单必须有样例（至少1条）佐证，不能凭空列
- **留痕**：`log.md` 追加 `[时间] MAU-2 完成，项目缺X个，标准类型缺Y个`
- **⛔ 卡点 1**：**唐总审阅 `enum_gaps.md`**，确认要扩的枚举值清单

#### MAU-3 · 扩枚举（项目 / 标准类型）
- **输入**：MAU-2 清单 + 唐总签字
- **动作**：`lark-cli base +field-update` 一次性扩展
- **产出**：`migration/schema_changelog.md` 追加本次扩展记录
- **验收**：`feishu_bitable_list_fields` 返回的 options 包含所有新值
- **留痕**：同步更新 `directory_schema_v1.json`（如该文件已存在）

#### MAU-4 · 字段映射器（生成脚本）
- **输入**：17.1 规则表 + 17.2 已扩枚举 + 17.3.MAU-3 新扩枚举
- **动作**：写 `migration/02_map.py`：
  1. 读 `raw_old_table.jsonl`
  2. 对每条记录逐字段映射（板块去序号、阶段映射、状态保留、标准类型推断、分类链重拼）
  3. 输出 `mapped_records.jsonl`（已对齐主表 schema 的记录）
  4. 输出 `02_mapping.csv`（人看的审阅版：旧值 / 映射后 / 档位 A/B/C）
  5. 档位规则：A=全字段齐全可直灌；B=有1-2个字段推断；C=标准类型等关键字段无法推断
- **产出**：`migration/02_map.py` + `mapped_records.jsonl` + `02_mapping.csv`
- **验收**：`mapped_records.jsonl` 行数 == 1204；CSV 三档之和 == 1204
- **留痕**：`log.md` 追加 `[时间] MAU-4 完成，A/B/C=X/Y/Z`

#### MAU-5 · 本批次内查重
- **输入**：`mapped_records.jsonl` + 本地 PDF 实体
- **动作**：
  1. 对 1,204 条按 `标准编号` 做批内去重（有同编号的归组）
  2. 对每条计算 `本地路径` 对应 PDF 的 SHA256（若 `os.path.exists` 为真）
  3. 标注批内重复（同 Hash / 同编号）
- **产出**：`migration/batch_dedup_report.md`（批内重复清单 + Hash 落盘到 `batch_hash_index.jsonl`）
- **验收**：报告可读；Hash 文件行数 ≤ 1204（跳过不存在路径）
- **留痕**：`log.md` 追加 `[时间] MAU-5 完成，批内去重后Z条`

#### MAU-6 · 与主表跨批查重
- **输入**：`mapped_records.jsonl`（批内去重后）+ 主表 1,313 条现有记录
- **动作**：
  1. 拉取主表现有记录的 `标准名称` + `标准编号`
  2. 对每条候选记录做三层匹配：
     - 标准编号精确匹配 → 标注 `版本更新` 或 `完全重复`
     - 标题 85% 相似度 → 标注 `疑似重复`（进入人工池）
     - 全不命中 → 标注 `新增`
  3. 特殊：主表本地路径为空，旧表有路径 → 视为"回填路径"，单独一档
- **产出**：`migration/cross_dedup_report.md` + `to_insert.jsonl`（新增）+ `to_update.jsonl`（回填主表路径）+ `review_list.csv`（疑似重复）
- **验收**：四档记录数相加 == 1204
- **留痕**：`log.md` 追加 `[时间] MAU-6 完成，新增X/更新Y/疑似Z/重复W`
- **⛔ 卡点 2**：**唐总审阅跨批查重报告**，决定"疑似重复"如何处理（收/弃），签字继续

### 阶段二：试点灌表（30 分钟）

#### MAU-7 · 试点 20 条
- **输入**：`to_insert.jsonl` 前 20 条（含 4.22×10 + 政策×10）
- **动作**：
  1. 调 `batch_create`（每批 100，此批 20）写入主表，保留原枚举值
  2. 每条返回 `new_record_id` 落盘
- **产出**：`migration/pilot_log.jsonl`（20 条完整日志：源record_id + new_record_id + 写入时间 + 状态）
- **验收**：20 条全部 `code=0`
- **留痕**：`log.md` 追加 `[时间] MAU-7 完成，试点 20 条成功`
- **⛔ 卡点 3**：**唐总肉眼抽查主表 5 条**，确认字段对齐 / 分类链正确 / 本地路径有效，签字继续

### 阶段三：批量灌表 + 回填（2 小时）

#### MAU-8 · 批量灌入"新增"记录
- **输入**：`to_insert.jsonl` 剩余记录（扣除试点 20 条）
- **动作**：
  1. `batch_create`，每批 100 条，批间 sleep 2s
  2. 失败自动重试 1 次，仍失败 → `failed_records.jsonl`
- **产出**：`migration/import_log.jsonl`（每批的 code + record_id 列表）
- **验收**：成功率 ≥ 99%，失败条数 < 10
- **留痕**：`log.md` 追加 `[时间] MAU-8 完成，灌入N条/失败F条`

#### MAU-9 · 批量回填"主表路径"
- **输入**：`to_update.jsonl`
- **动作**：`batch_update`（`lark-cli api POST .../records/batch_update`），只更新 `本地路径` / `文件大小MB` / `文件存在` / `备注` 四个字段
- **产出**：续写 `import_log.jsonl`
- **验收**：主表被更新记录数 == `to_update.jsonl` 行数
- **留痕**：`log.md` 追加 `[时间] MAU-9 完成，回填U条路径`

#### MAU-10 · 疑似重复 / C 档人工清单
- **输入**：`review_list.csv` + C 档记录
- **动作**：导出 Excel，每行附原数据 + AI 建议 + 空的"决策列"
- **产出**：`migration/人工审阅清单.xlsx`
- **⛔ 卡点 4**：**唐总决定每条"灌入 / 丢弃 / 挂起"**，返回后按决策补灌

### 阶段四：校验收尾（30 分钟）

#### MAU-11 · 三重校验
- **输入**：主表当前数据
- **动作**：
  1. **数量校验**：主表总行数 == 1313（原） + MAU-8 新增数 + MAU-10 补灌数；同时 `批次=4.22标准 OR 政策监控` 的行数应 == 1204 - 批内去重扣减 - 跨批重复扣减
  2. **分类链一致性**：随机抽 50 条，检查 `分类链` 字段 == `板块/项目/阶段/标准类型` 拼接值
  3. **本地路径真实性**：抽 100 条 `本地路径` 非空记录，`os.path.exists` 应 100% True
- **产出**：`migration/verification_report.md`
- **验收**：三项全绿，或明确列出异常条目 + 根因

#### MAU-12 · 封版交付
- **输入**：`verification_report.md` + 本章所有 log
- **动作**：生成 `migration/DONE.md`：最终数字、灌入/回填/丢弃条数、遗留问题、唐总签字栏
- **产出**：`DONE.md`
- **⛔ 卡点 5**：**唐总签字 → 第一件事完工 → 启动第二件事（baseline.sqlite 基准库）**

## 17.4 状态表（每个 MAU 完成后必须在此更新）

| MAU | 状态 | 完成时间 | 产出文件 | 备注 |
|:---:|:---|:---:|:---|:---|
| MAU-0 枚举扩3项 | ✅ | 2026-05-09 06:35 | schema_changelog（待建） | 入库状态+板块+阶段 |
| MAU-1 dump | ⏳ | — | raw_old_table.jsonl | — |
| MAU-2 枚举扫描 | ⏳ | — | enum_gaps.md | ⛔ 卡点 1 |
| MAU-3 扩枚举 | ⏳ | — | schema_changelog.md | — |
| MAU-4 字段映射 | ⏳ | — | mapped_records.jsonl | — |
| MAU-5 批内查重 | ⏳ | — | batch_dedup_report.md | — |
| MAU-6 跨批查重 | ⏳ | — | cross_dedup_report.md | ⛔ 卡点 2 |
| MAU-7 试点20条 | ⏳ | — | pilot_log.jsonl | ⛔ 卡点 3 |
| MAU-8 批量新增 | ⏳ | — | import_log.jsonl | — |
| MAU-9 回填路径 | ⏳ | — | import_log.jsonl | — |
| MAU-10 人工清单 | ⏳ | — | 人工审阅清单.xlsx | ⛔ 卡点 4 |
| MAU-11 校验 | ⏳ | — | verification_report.md | — |
| MAU-12 封版 | ⏳ | — | DONE.md | ⛔ 卡点 5 |

**状态符号**：⏳ 待启动 / 🔄 进行中 / ✅ 完成 / ⚠️ 异常待处理 / ⛔ 等决策人

## 17.5 工作留痕规范（铁律）

**任何 AI 完成一个 MAU 后，必须做到 3 件事**：

1. **产出物落盘**：17.3 规定的文件必须真实生成在 `~/.openclaw/workspace/migration/`，不得只在对话里描述
2. **更新状态表**：直接 `edit` 本章 17.4 的状态表，把 ⏳ 改成 ✅ 并填完成时间
3. **追加日志**：在 `~/xiaoqiu-obsidian-vault/log.md` 追加一条，格式：
   ```markdown
   ## [YYYY-MM-DD HH:MM] migration | MAU-X 完成
   - 执行者：AI名称 / 子代理ID
   - 产出：migration/xxx.jsonl（行数 N）
   - 验收：✅ 全绿 / ⚠️ 部分异常（详见 xxx.md）
   - 下一步：进入 MAU-(X+1) / 等唐总卡点 N 签字
   ```

**不允许**：跳步、只口述不产文件、不更新状态表、不写 log 就说"完成了"。

## 17.6 异常与回滚

| 异常类型 | 处理 |
|---|---|
| `lark-cli` 写入返回 code != 0 | 记入 `failed_records.jsonl`，不阻断批次，收尾统一 review |
| 枚举值未扩就灌 → 字段写入失败 | 立即停，回 MAU-3 扩枚举 |
| 本地路径不存在 | `文件存在=false`，备注写"路径失效，待核"，不阻断 |
| 灌表后发现映射规则错 | 用 `batch_delete` 按 `new_record_id` 回滚 MAU-8 产出，修规则重跑 |
| 唐总卡点未签字就往下走 | **绝对禁止**，停工并重新请示 |

## 17.7 当前状态（初始化）

- ✅ MAU-0：入库状态 / 板块 / 阶段 三项枚举已扩
- ⏳ MAU-1：待启动
- 下一步动作：**启动子代理执行 MAU-1 ~ MAU-6**，产出到 `migration/` 后回到主对话找唐总签 ⛔ 卡点 1 和卡点 2

---

# 十八、版本历史

| 版本 | 日期 | 修订内容 | 执行人 |
|---|---|---|---|
| V0.1 | 2026-05-08 | 初稿：Pipeline 架构 + 第0批次技术手册 | 唐泽龙 |
| V0.2 | 2026-05-09 | 修订第十六章「后续批次执行指引」冲突表述（由"新建表"改为"统一入主表，先独立查重"）；新增第十七章「旧表迁移 MAU SOP」；更新工作脚本位置表；新增枚举扩展规范；新增版本历史表 | 小琳（基于唐总 2026-05-09 06:14 / 06:29 / 06:40 三次指示） |
